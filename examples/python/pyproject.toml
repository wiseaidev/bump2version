[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "my-python-app"
version = "0.1.0"
requires-python = ">=3.12"
description = "Example Python project whose version is managed by bump2version"
license = { text = "MIT" }
