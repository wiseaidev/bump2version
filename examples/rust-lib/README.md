# my-rust-lib

> version = "0.1.0"

A sample Rust library whose version is managed by `bump2version`.

## Bump the version

```sh
# From the project root
bump --config-file examples/rust-lib/.bumpversion.toml --bump patch

# Or from inside this directory
cd examples/rust-lib
bump --bump patch
```
