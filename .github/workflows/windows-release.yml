name: Windows Release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release --features rust-binary

      - name: Upload binary to GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: pwsh
        run: |
          if ("${{ github.ref_type }}" -eq "tag") {
            $TAG = "${{ github.ref_name }}"
          } else {
            $TAG = gh release list --limit 1 --json tagName --jq '.[0].tagName'
          }

          if (-not $TAG -or $TAG -eq "null") {
            Write-Host "No release tag found. Skipping upload."
            exit 0
          }

          Write-Host "Uploading to release: $TAG"
          gh release upload $TAG target/release/bump.exe --clobber
